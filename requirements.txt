# Core
requests>=2.28.0
pyyaml>=6.0
urllib3>=1.26.0

# Archive support (optional but recommended)
py7zr>=0.20.0      # for .7z
rarfile>=4.0       # for .rar (requires unrar/winrar installed)
# patool>=1.12     # alternative multi-format (optional, but can replace above)

# GUI (tkinter is built-in on Windows, but may need separate install on Linux)
# On Linux, run: sudo apt-get install python3-tk
